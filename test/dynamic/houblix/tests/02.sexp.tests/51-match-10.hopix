let test =
    switch (K (K ("OK"))) {
    | K (x & K (y)) ->
      switch (x) {
      | K (z) ->
         print_string ("This test is ");
         print_string (y);
         print_string (".\n")
      }
    }