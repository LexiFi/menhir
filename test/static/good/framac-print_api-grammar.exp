Grammar has 3 nonterminal symbols, among which 1 start symbols.
Grammar has 5 terminal symbols.
Grammar has 7 productions.
nullable(word) = false
nullable(type_string) = false
nullable(main) = false
first(word) = WORD
first(type_string) = WORD LPAR
first(main) = WORD LPAR
minimal(word) = (* 1 *) WORD
minimal(type_string) = (* 1 *) WORD
minimal(main) = (* 2 *) WORD EOF
maximal(word) = 1
maximal(type_string) = infinity
maximal(main) = infinity
follow(word) = WORD RPAR EOF COMMA
follow(type_string) = WORD RPAR EOF COMMA
follow(main) = #
Built an LR(0) automaton with 21 states.
The grammar is SLR(1).
The construction mode is pager.
Built an LR(1) automaton with 21 states.
8 out of 21 states have a default reduction.
The action table is 126 entries; 25 non-zero; 33 compressed.
The action table occupies roughly 128 bytes.
The goto table is 84 entries; 19 non-zero; 36 compressed.
The goto table occupies roughly 128 bytes.
The error table occupies roughly 48 bytes.
The default_reduction table occupies roughly 48 bytes.
The lhs table occupies roughly 40 bytes.
The trace table occupies roughly 8 bytes.
