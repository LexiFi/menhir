Grammar has 3 nonterminal symbols, among which 1 start symbols.
Grammar has 6 terminal symbols.
Grammar has 7 productions.
nullable(main) = false
nullable(list(expr)) = true
nullable(expr) = false
first(main) = LPAREN INT EOL
first(list(expr)) = LPAREN INT
first(expr) = LPAREN INT
minimal(main) = (* 1 *) EOL
minimal(list(expr)) = (* 0 *)
minimal(expr) = (* 1 *) INT
maximal(main) = infinity
maximal(list(expr)) = infinity
maximal(expr) = infinity
follow(main) = #
follow(list(expr)) = EOL
follow(expr) = TIMES RPAREN PLUS LPAREN INT EOL
Built an LR(0) automaton with 14 states.
The grammar is not SLR(1) -- 2 states have a conflict.
The construction mode is pager.
Built an LR(1) automaton with 14 states.
4 shift/reduce conflicts were silently solved.
6 out of 14 states have a default reduction.
The action table is 98 entries; 24 non-zero; 27 compressed.
The action table occupies roughly 112 bytes.
The goto table is 56 entries; 8 non-zero; 22 compressed.
The goto table occupies roughly 104 bytes.
The error table occupies roughly 48 bytes.
The default_reduction table occupies roughly 40 bytes.
The lhs table occupies roughly 40 bytes.
The trace table occupies roughly 8 bytes.
